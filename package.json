{
  "name": "armenian-language-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build:embed": "node scripts/build-embed.mjs",
    "check": "npm test && npm run build:embed"
  },
  "engines": {
    "node": ">=22"
  }
}
